{
    "scripts": {
        "prepare-client": "cd client && yarn install && yarn build ",
        "prepare-server": "cd Server && yarn install",
        "heroku-postbuild": "yarn prepare-server && yarn prepare-client ",
        "start": "cd Server && yarn start"
    },

    "dependencies": {}
}